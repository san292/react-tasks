import React from 'react';
import Button from './Button';

const Header = () => {
  return (
    <div className="header">
      <h1>react-tasks</h1>
      <Button />
    </div>
  );
};

export default Header;
